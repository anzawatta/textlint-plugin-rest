{
  "name": "textlint-plugin-rest",
  "description": "reStructuredText support for textlint",
  "version": "0.1.1",
  "main": "lib/index.js",
  "files": [
    "src",
    "lib"
  ],
  "scripts": {
    "build": "babel -s -d lib src",
    "watch": "babel -w -s -d lib src",
    "prepublish": "npm run --if-present build",
    "clean": "rm -frv lib",
    "test": "mocha"
  },
  "author": "anzawatta",
  "email": "mailfrom@dawtrav.skr.jp",
  "license": "MIT",
  "keywords": [
    "textlint",
    "plugin",
    "rst",
    "reStructuredText"
  ],
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-plugin-add-module-exports": "^0.1.2",
    "babel-preset-es2015": "^6.6.0",
    "babel-register": "^6.8.0",
    "mocha": "^3.5.3",
    "power-assert": "^1.3.1",
    "textlint": "^11.6.3",
    "@textlint/ast-tester": "^2.1.6"
  },
  "dependencies": {
    "debug": "^2.2.0",
    "structured-source": "^3.0.2",
    "traverse": "^0.6.6"
  }
}
